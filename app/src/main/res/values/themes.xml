<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryVariant</item>
        <item name="colorAccent">@color/secondary</item>
        <!--<item name="colorOnPrimary">@color/colorOnPrimary</item>-->
    </style>

    <style name="Theme.PayWingsKycSDKSampleApp.Starting" parent="Theme.SplashScreen">
        // Set the splash screen background, animated icon, and animation duration.
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>

        // Use windowSplashScreenAnimatedIcon to add either a drawable or an
        // animated drawable. One of these is required.
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>

        // Set the theme of the Activity that directly follows your splash screen.
        <item name="postSplashScreenTheme">@style/Theme.PayWingsKycSDKSampleApp</item>  # Required.
    </style>


    <style name="Theme.PayWingsKycSDKSampleApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/primaryVariant</item>
        <item name="android:datePickerDialogTheme">@style/Theme.PayWingsKycSDKSampleApp.Dialog</item>
        <item name="android:timePickerDialogTheme">@style/Theme.PayWingsKycSDKSampleApp.Dialog</item>
    </style>


    <style name="Theme.PayWingsKycSDKSampleApp.Dialog" parent="android:Theme.Material.Light.Dialog">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorPrimaryDark">@color/primaryVariant</item>
        <item name="android:colorAccent">@color/secondary</item>
    </style>
</resources>